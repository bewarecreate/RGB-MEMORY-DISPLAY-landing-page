BEWARE RGB MEMORY DISPLAY — SERVER READY / ADAPTIVE

업로드 방법
1. 이 압축을 풉니다.
2. index.html / style.css / script.js / assets 폴더를 서버의 공개 루트(public_html, www, htdocs 등)에 그대로 업로드합니다.
3. 별도의 npm, 빌드, DB, 서버 프로그램은 필요하지 않습니다.

적응형 레이아웃
- Wide Desktop: 1600px 이상
- Desktop: 1280–1599px
- Wide Tablet: 1024–1279px
- Tablet: 768–1023px
- Mobile: 767px 이하
- Narrow Mobile 안전 보정: 389px 이하

각 화면 구간마다 콘텐츠 폭, 폰트 크기, 카드 크기, 패딩, 2열/1열 배치를 별도로 지정했습니다.
따라서 viewport 폭에 따라 텍스트가 계속 비례 축소되는 단순 유동형이 아니라 기기군별로 디자인이 전환되는 적응형 구조입니다.

이미지
- 대형 이미지는 AVIF 우선 + WebP fallback 방식으로 제공됩니다.
- 첫 화면 이미지는 preload/fetchpriority=high를 사용합니다.
- 아래쪽 이미지는 lazy loading/async decoding을 사용합니다.

폰트
- Pretendard는 jsDelivr CDN에서 로드됩니다.
- 외부 인터넷 연결이 없는 폐쇄형 서버에서는 시스템 한글 폰트 fallback이 사용됩니다.

파일 구조
/index.html
/style.css
/script.js
/assets/*


[v5] Section 02–10 main title line-height reduced by 10% across adaptive breakpoints.

[2026-08-07 update]
Section 6 showcase is now HTML/CSS based. Only the three right-side product visuals are image assets, optimized as AVIF/WebP.

[Section spacing]
- S01/S02 spacing is preserved.
- From S03 onward, section boundary spacing is normalized to the current S06→S07 visual gap at each adaptive breakpoint.

[Adaptive v11]
- Desktop 1280px+ layout preserved.
- Tablet/mobile shells, sticky story, horizontal showcase, comparison cards, carousels and accordions refined.
- Mobile height-only browser chrome resize no longer recalculates Section 05 horizontal progress.
